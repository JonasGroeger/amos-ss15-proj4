spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost/<prod-db>
    username: <prod-username>
    password: <prod-password>
  jpa:
    database: POSTGRESQL
    hibernate:
      naming_strategy: org.hibernate.cfg.ImprovedNamingStrategy
      ddl-auto: update
